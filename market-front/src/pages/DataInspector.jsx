import React from 'react';\nimport { useSelector } from 'react-redux';\n\nconst DataInspector = () => {\n  const { user } = useSelector((state) => state.account);\n  const { cart } = useSelector((state) => state.cart);\n\n  const checkLocalStorage = () => {\n    const keys = Object.keys(localStorage);\n    const data = {};\n    keys.forEach(key => {\n      data[key] = localStorage.getItem(key);\n    });\n    return data;\n  };\n\n  const checkSessionStorage = () => {\n    const keys = Object.keys(sessionStorage);\n    const data = {};\n    keys.forEach(key => {\n      data[key] = sessionStorage.getItem(key);\n    });\n    return data;\n  };\n\n  return (\n    <div style={{ padding: '20px', fontFamily: 'monospace', fontSize: '12px' }}>\n      <h3>🔍 Data Inspector (Logout Test)</h3>\n      \n      <div style={{ marginBottom: '20px' }}>\n        <h4>Redux State:</h4>\n        <div style={{ background: '#f5f5f5', padding: '10px', borderRadius: '4px' }}>\n          <strong>User:</strong> {user ? JSON.stringify(user, null, 2) : 'null'}<br/>\n          <strong>Cart:</strong> {cart ? JSON.stringify(cart, null, 2) : 'null'}\n        </div>\n      </div>\n\n      <div style={{ marginBottom: '20px' }}>\n        <h4>LocalStorage:</h4>\n        <div style={{ background: '#e8f5e8', padding: '10px', borderRadius: '4px' }}>\n          <pre>{JSON.stringify(checkLocalStorage(), null, 2)}</pre>\n        </div>\n      </div>\n\n      <div style={{ marginBottom: '20px' }}>\n        <h4>SessionStorage:</h4>\n        <div style={{ background: '#f0f8ff', padding: '10px', borderRadius: '4px' }}>\n          <pre>{JSON.stringify(checkSessionStorage(), null, 2)}</pre>\n        </div>\n      </div>\n\n      <div style={{ marginBottom: '20px' }}>\n        <h4>Cookies:</h4>\n        <div style={{ background: '#fff5ee', padding: '10px', borderRadius: '4px' }}>\n          <pre>{document.cookie || 'No cookies'}</pre>\n        </div>\n      </div>\n\n      <div style={{ marginTop: '20px', padding: '10px', background: '#ffebee', borderRadius: '4px' }}>\n        <strong>⚠️ Test Instructions:</strong><br/>\n        1. Login olun<br/>\n        2. Sepete bir şeyler ekleyin<br/>\n        3. Bu sayfayı açık tutun<br/>\n        4. Logout yapın<br/>\n        5. Bu sayfada tüm veriler temizlenmiş olmalı\n      </div>\n    </div>\n  );\n};\n\nexport default DataInspector;